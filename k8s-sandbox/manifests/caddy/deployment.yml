apiVersion: apps/v1
kind: Deployment
metadata:
  name: caddy
  namespace: forge
  labels:
    k8s-sandbox.dev/role: edge
spec:
  replicas: 1
  selector:
    matchLabels:
      k8s-sandbox.dev/role: edge
  template:
    metadata:
      labels:
        k8s-sandbox.dev/role: edge
    spec:
      automountServiceAccountToken: false
      containers:
        - name: caddy
          image: caddy:2
          command:
            - caddy
          args:
            - run
            - --config
            - /etc/caddy/Caddyfile
            - --adapter
            - caddyfile
          ports:
            - name: https
              containerPort: 443
          volumeMounts:
            - name: caddy-config
              mountPath: /etc/caddy
              readOnly: true
            - name: wildcard-certs
              mountPath: /etc/certs
              readOnly: true
      volumes:
        - name: caddy-config
          configMap:
            name: caddy-config
            items:
              - key: Caddyfile
                path: Caddyfile
        - name: wildcard-certs
          secret:
            secretName: wildcard-forge-harshitrv-com-tls
